# ============================================================
# requirements.txt
# Web App / Deployment Dependencies (CPU only, for Render)
# Install with: pip install -r requirements.txt
# For GPU training, use: pip install -r requirements-train.txt
# ============================================================

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
jinja2==3.1.2
aiofiles==23.2.1

# Deep learning — CPU only (fast install on Render)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu

# Data handling
numpy>=1.23.0,<2.0.0
pillow>=9.0.0
pandas>=1.5.0

# Metrics
scikit-learn>=1.2.0
matplotlib>=3.5.0
tqdm>=4.64.0

# Computer vision (headless for server)
opencv-python-headless>=4.7.0
